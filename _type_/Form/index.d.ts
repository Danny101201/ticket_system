type LoginFormType = {
  email: string,
  password: string
}
type AddNewFormType = {
  subject: string,
  issue: string,
  details: string,
}
type InputType = 'text'|'button' | 'checkox' | 'color' | 'password' | 'email' | 'file' | 'date' | 'number'